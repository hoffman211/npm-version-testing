/*
 * Complete Configuration
 *
 * See all options:
 */
module.exports = function( grunt ) {

		grunt.registerTask('complete', 'build complete', function() {
    
    	grunt.log.writeln('The build is complete. Please commit the tag');
  	
  	});		

};